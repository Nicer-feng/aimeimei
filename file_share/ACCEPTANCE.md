# 文件分享中心 v0.1.0 验收记录

2026-09-14 16:57 已上线：https://feng.asia/admin/share 。

## 本地回归

隔离测试覆盖登录、跨管理员隔离、分片上传、密码重设、暂停恢复、回收站恢复和永久删除。
8 个并发下载仅放行 3 个；16 个并发页面访问仅放行 10 个；30 分钟 UV 去重正确。
路径穿越、HTML/SVG 预览、伪造文件类型均被拦截。V4 签名、私有 ACL 与匿名读取检查、备份脱敏、旧产品路由通过。

## 真实生产闭环

1. 用户在独立 Chrome 窗口正常登录管理员账号；未读取/代填密码，未绕过登录。
2. PNG（108849 字节）、MP4（25033 字节）、PDF（13183 字节）实际上传至现有 OSS。
3. 三者对象类型正确、ACL 私有、匿名请求返回403。
4. 创建三个文件的7天密码分享，二维码PNG下载成功。
5. 独立未登录浏览器输入密码后显示文件。
6. 图片真实解码；视频实际播放并拖动至第4秒，收到HTTP206。
7. PDF iframe加载，OSS返回application/pdf；保留浏览器原生打开入口。
8. 下载内容与上传原文件逐字节一致。
9. 后台记录 VIEW_SHARE=1、PASSWORD_SUCCESS=1、PREVIEW_FILE=3、DOWNLOAD_FILE=1，IP有值；计数访问1、下载1。
10. 撤回后原链接刷新立即失效，原对象仍私有存在且文件状态READY。
11. 已重新创建分享验证复用，并撤回两条测试分享。

三个无业务数据的验收样例保留在后台，可自行使用或移入回收站。
浏览器脚本错误0，390×844手机视口无横向溢出。未测试实体iPhone/Android；PDF原生渲染行为取决于浏览器。
未登录管理API返回401，随机分享码404，AI/主页/小猫书入口和健康接口正常。AI版本仍为v2.24.2。

## 部署与证据

- 备份：/opt/ai-platform-release-backups/file-share-deploy-20260914-165716/
- 浏览器报告、截图和下载比对：/private/tmp/file-share-production-qa/
- 回归命令：python3 file_share/tests/run.py

之前Bucket查询403的具体原因是禁用V1 URL签名，现已改用官方SDK V4。
用户补充RAM并修改Bucket Policy/CORS后，真实私有访问与直传验收通过，早期临时对象已清理。

## v0.1.1 验证

隔离环境验证：管理员归属、改名非法路径/扩展名校验、原分享同步名称、搜索、回收站禁止改名；已有并发限额、密码和私有存储回归通过。Chrome 验证类型/排序选择、键盘选择、弹窗内下拉、Escape、重命名和版本历史，390px手机布局无横向溢出。

## v0.1.2 验证

隔离接口回归通过。Chrome模拟新版本验证：当前版本不提示、新构建提示、稍后提醒、更新构建重新提示、上传期间阻止刷新、上传结束可刷新；手机尺寸已检查。已部署并核对公网构建号、更新提示DOM、静态资源哈希和基础路由；新版本提示交互由隔离浏览器模拟验证。

## v0.1.3 验证

真实适配器查询公开测试IP成功返回China/Jiangsu/Nanjing。隔离测试覆盖异步查询、同IP去重、缓存、内网IPv4/IPv6跳过、429退避和缓存备份脱敏，已有分享回归通过。浏览器模拟待查询到成功自动更新，确认英文列、原IP保留、来源标注、手动刷新及离开列表停止轮询；未使用真实访客数据。部署后以生产服务账号验证公开测试IP归属地与缓存成功，公网页面构建和资源哈希一致，匿名访问日志接口返回401。

## v0.1.4 验证

浏览器验证登录页、后台六个菜单、外部失效分享页备案号码、链接及图标；桌面及390px手机尺寸无横向溢出。外部正常与密码页共用同一静态页脚。已上线，公网后台、外部分享页及公安图标检查通过。
