# feng.asia 共仓管理约定

继续使用现有 aimeimei Git 仓库，不为独立产品新建仓库，也不为每个产品维护长期分支。产品独立不等于必须拆仓库。

## 产品边界

| 产品 | 主要代码 | 版本与记录 | 后续发布标签 |
|---|---|---|---|
| AI槑槑 | ai.html、res/ai.*、ai_platform/ | 根目录 VERSION、BUILD_ID、CHANGELOG.md | ai/v版本号 |
| 文件分享中心 | file_share/、res/file-share/ | file_share/VERSION、BUILD_ID、CHANGELOG.md | file-share/v版本号 |
| 小猫书 | cat.html、ai_platform/handlers/cats.py | 尚未独立版本化，下次功能发布时建立 products/cat/ 下的版本和更新记录 | cat/v版本号 |
| 主页 | index.html 及引用资源 | 尚未独立版本化，下次功能发布时建立 products/home/ 下的版本和更新记录 | home/v版本号 |
| 共用基础设施 | app.py、数据库连接、infrastructure/、deploy/ | 在受影响产品的更新记录中说明 | 随对应产品发布 |

根目录 VERSION 继续表示 AI槑槑版本，不代表整个网站。保留历史标签，不重写历史。不为小猫书和主页虚构已发布版本；无需现在移动现有代码目录。

## 开发与提交

- main 保持可发布；较大改动使用短期分支，例如 feat/file-share-rename，完成后合回 main。
- 提交按业务组织，中文约定格式：feat(file-share):新增文件重命名、fix(cat):修复照片加载、docs(repo):完善共仓管理说明。
- 一次需求跨共用基础设施和产品时，可放在同一提交，说明受影响范围。
- 不提交运行数据库、密钥、会话、上传文件和本地验收输出。用户临时素材需要明确选入，不能顺手 git add 全目录。

## 持续部署授权

用户于2026-09-14明确要求：以后这里做完就直接上线部署。本授权适用于本仓库feng.asia项目的后续常规需求：实现、验证通过、备份、部署（含必要服务重启）、公网检查和Git同步应直接完成，不再逐次询问是否部署。之前因缺少单次部署确认而暂停的记录仅为历史，不再作为重复确认理由。

## 发布

1. 只更新本次受影响产品的 VERSION、BUILD_ID、CHANGELOG.md 和资源缓存参数。
2. 测试受影响业务；改动 app.py、登录、数据库、存储或代理时，检查所有受影响产品。
3. 提交并推送代码。备份生产文件后，只部署本次变更清单，核对服务与公网页面。
4. 将产品发布标签指向对应提交，例如 file-share/v0.1.1；标签代表当次完整仓库快照，通过名称识别发布产品。
5. 版本号独立不代表运行进程隔离。目前 AI槑槑、小猫书和文件分享共用 Python 服务与 SQLite，后端发布重启会短暂影响它们；主页由 Caddy 单独提供。
6. 回退优先恢复对应发布的代码备份，不回退整个仓库或覆盖数据库；生产回退仍须明确确认。

暂时不引入多仓库、子模块、复杂工作区工具或统一大版本。等某个产品确实需要独立服务、依赖或发布节奏，再拆部署单元。
